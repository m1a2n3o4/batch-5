// Arrow function '=>'
//NaN (Not a Number) - Error

function add(a,b) {
   // console.log(a + b);
   return a + b;
}

// Arrow function syntax.
const add2 =  (a,b) =>  {
    return a + b;

    const add3 = () => {
        // write the code
    }

};

// Implicit Arroe with no Return keyword.
const subtract = (a,b) => a - b;

// Can leave off() with a single paras
const double = (a) => a * 2;



console.log(add(1,2));
console.log(add2(1,2));
console.log(subtract(5,4));
console.log(double(10));
