## React is JS Library/framework. (Anguala,Vuejs, Nuxt js).

## Uses.
 - Built high level application.
 - More secure.
 - Single page application. (Routers).
 - State manage menet system. (Complete data (JSON-arrays-object)).
 - Redux system/redux store (cart, filter).
 - Mobile Applications.
 - Webapp;ication - Mobile App. (React Nartive)

# Coding wise.
  - you mix, 3 lanagues (HTML + JS + CSS).

# High levele replaceble to DOM
  - 15 lines (passing data from JS to HTML).
  - React 3 lines of code in same page.
  - We can implement easly AUTH operation (user/password.).
  - React Library/Framework
        

## You understand and Practice.

# we will react along with a PRJOCT + Bootstrap.
# Project wise topics learning.
# Netflix.
# API calles.

# Fire base - syste. (Malking real API calls).
# Build and Deploye application...

# We need download the opensource framework/lib.
# we will all framework after download.
# Completly RUN with comand prom.
# To run React App - we need local server
# we need react with a server.
# To download frame, to run application, to run server, to install any plugin, single app, to redux.....CMD PROM.
# 10 to 15 commnds.... (NPM comands)


# How to start.
1) Node is installed or not ? ( node -v)
2) Check NPM is instleed ot not ? (npm -v).
3) RUN : npx create-react-app react-new-project
