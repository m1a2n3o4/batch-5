// setInterval - 
 const interval = setInterval(mycalback, 2000);

function mycalback() {
    console.log(Date());
   // clearInterval(interval);
};

setInterval( function() {
    // console.log('test');
}, 1000);

// It will run untill we stop the interval by using a method clearInterval.

// clearInterval(interval);

function stopinterval() {
    clearInterval(interval);
}

// Setout time function ?
// SetInteval function.
// Flipkart offert - vivo 12 pron avaiable for exatly 11AM and will abaoble only 1 sec.
// Tatakal ticket booking - 11AM. setTImout(bookTatakalsleeper, 11AM);

function bookTatakalsleeper() {
    // Ticket Booking logic.
};



